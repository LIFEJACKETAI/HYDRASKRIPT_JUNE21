# ─────────────────────────────────────────────
# HydraSkript — Environment Variables Template
# Copy this file to .env.local and fill in your keys
# cp .env.example .env.local
# ─────────────────────────────────────────────

# ── DATABASE ──────────────────────────────────
# Local dev: SQLite (default, no setup needed)
# DATABASE_URL="file:./dev.db"

# Production: PostgreSQL (Supabase recommended)
DATABASE_URL="postgresql://postgres:***@db.<PROJECT_REF>.supabase.co:5432/postgres"

NEXT_PUBLIC_SUPABASE_URL="https://<PROJECT_REF>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<ANON_KEY>"
SUPABASE_SERVICE_ROLE_KEY="<SERVICE_ROLE_KEY>"
SUPABASE_STORAGE_BUCKET="hydraskript-assets"

# Prisma Connection Pool Tuning (Critical for Supabase)
# Supabase Free: ~60 connections, Pro: 100-500, Team: 500+
# Set pool max based on your plan. Formula: max = (plan_limit * 0.7) / instances
DATABASE_POOL_MAX="10"
PRISMA_TRANSACTION_TIMEOUT="15000"

# ── AI / LLM ──────────────────────────────────
# NVIDIA NIM — PRIMARY provider for all book/chapter text generation
# Get your key at: https://build.nvidia.com
# "Nemotron 3.5 Lightning" is served as a free-to-prototype endpoint at:
#   https://build.nvidia.com/nvidia/nemotron-3.5-lightning-30b-a3b
NVIDIA_NIM_API_KEY="<YOUR_NVIDIA_NIM_KEY>"
NVIDIA_NIM_API_URL="https://integrate.api.nvidia.com/v1/chat/completions"
NVIDIA_NIM_MODEL="nvidia/nemotron-3.5-lightning-30b-a3b"

# OpenRouter — BACKUP provider (used on fallback if NVIDIA NIM fails)
# Get your key at: https://openrouter.ai/keys
# NOTE: free models use a `:free` suffix (there is no model named "openrouter/free").
# List current free models with:
#   curl https://openrouter.ai/api/v1/models | jq '.data[].id' | grep ':free'
OPENROUTER_API_KEY="<YOUR_OPENROUTER_KEY>"
OPENROUTER_MODEL="google/gemma-4-31b-it:free"

# Google AI / Gemini — text generation fallback (Gemini 2.5 Flash)
GOOGLE_AI_API_KEY="<YOUR_GOOGLE_AI_KEY>"
GEMINI_TEXT_MODEL="gemini-2.5-flash"

# Z.AI — LLM provider
Z_AI_API_KEY="<YOUR_ZAI_API_KEY>"

# ── IMAGE GENERATION ──────────────────────────
# Google Gemini Image Model
GEMINI_IMAGE_MODEL="gemini-2.5-flash-image"

# Stability AI (optional — for image generation fallback)
STABILITY_AI_API_KEY="<YOUR_STABILITY_KEY>"

# Google OAuth (for authentication)
GOOGLE_OAUTH_CLIENT_ID="<YOUR_GOOGLE_CLIENT_ID>"
GOOGLE_OAUTH_CLIENT_SECRET="<YOUR_GOOGLE_CLIENT_SECRET>"

# ── PAYMENTS ──────────────────────────────────
# Stripe — used for credit purchases
# Get your keys at: https://dashboard.stripe.com/api-keys
STRIPE_SECRET_KEY="<YOUR_STRIPE_SECRET_KEY>"
STRIPE_PUBLISHABLE_KEY="<YOUR_STRIPE_PUBLISHABLE_KEY>"
STRIPE_WEBHOOK_SECRET="<YOUR_STRIPE_WEBHOOK_SECRET>"

# Price IDs (create or reuse in Stripe Dashboard)
STRIPE_PRICE_STARTER="<price_id>"
STRIPE_PRICE_AUTHOR="<price_id>"
STRIPE_PRICE_PUBLISHER="<price_id>"
STRIPE_PRICE_STUDIO="<price_id>"
STRIPE_PRICE_PACK_100="<price_id>"
STRIPE_PRICE_PACK_500="<price_id>"
STRIPE_PRICE_PACK_1000="<price_id>"
STRIPE_PRICE_FOUNDER_399="<price_id>" # Founder Early Bird $399 one-time
STRIPE_PRICE_FOUNDER_499="<price_id>" # Founder Standard $499 one-time

# ── AUTH ──────────────────────────────────────
# NextAuth secret — generate with: openssl rand -base64 32
NEXTAUTH_SECRET="<GENERATED_SECRET>"
NEXTAUTH_URL="http://localhost:3002"

# ── FILE STORAGE (Production only) ────────────
# Cloudflare R2 — for persistent image/audio/file storage
#
# How to get these (Cloudflare dashboard):
#   1. R2 → "Create bucket" (name it e.g. "hydraskript-assets").
#   2. R2 → "Manage R2 API Tokens" → "Create API token" → permission
#      "Object Read & Write", scope to your bucket. Save the
#      Access Key ID + Secret Access Key (secret is shown only once).
#   3. R2_ACCOUNT_ID is your Cloudflare Account ID (shown on the
#      R2 overview page / in the account URL).
#   4. For images to load in the browser, enable PUBLIC access on the
#      bucket (Settings → Public access): either connect a Custom Domain
#      (recommended) or enable the r2.dev subdomain. Set R2_PUBLIC_URL
#      to that public base URL (no trailing slash).
#      If you skip this, exports still work (streamed via signed requests)
#      but cover/illustration images will not load.
R2_ACCOUNT_ID="<YOUR_R2_ACCOUNT_ID>"
R2_ACCESS_KEY_ID="<YOUR_R2_ACCESS_KEY_ID>"
R2_SECRET_ACCESS_KEY="<YOUR_R2_SECRET_ACCESS_KEY>"
R2_BUCKET_KEY="<YOUR_R2_BUCKET_NAME>"
R2_PUBLIC_URL="https://<YOUR_PUBLIC_DOMAIN_OR_R2DEV_URL>"

# ── APP ───────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3002"
NODE_ENV="development"